# Celest — 集成测试用例清单

> 手动测试用。每项测试后记录 实际结果 + 截图路径。
> 发现问题后在末尾「问题汇总」区追加。

---

## 测试环境

| 项目 | 值 |
|------|-----|
| 日期 | ____________ |
| 测试人 | ____________ |
| VS Code 版本 | ____________ |
| Celest 构建产物 | ext: ____ KB / gui: ____ KB |
| deepseek-tui 版本 | (Output → Celest → `ACP initialized:`) |
| OS | Windows 11 / 其他: ________ |

---

## 一、UI 布局

### 1.1 整体布局
- [x] 1.1.1 打开 Celest 面板后，从上到下依次看到：标题栏 → 空状态 → 输入区 → 状态栏
  - 预期：四层垂直排列，无重叠、无溢出
  - 实际：ok
- [x] 1.1.2 面板宽度缩窄至 300px 时，布局不崩坏
  - 预期：输入框自适应缩小，消息文字正常换行
  - 实际：ok

### 1.2 输入区域
- [x] 1.2.1 输入框最小高度约 60px（3 行），placeholder 文字可见
- [x] 1.2.2 输入多行文本（5 行以上），输入框自动增高但不超过 200px
- [x] 1.2.3 输入区底部显示 "Enter to send, Shift+Enter for newline" 提示
- [x] 1.2.4 Send 按钮位于输入区右下角

### 1.3 Stop 按钮
- [x] 1.3.1 空闲时 Stop 按钮不可见
- [x] 1.3.2 生成中 Stop 按钮出现在输入框右上角
- [x] 1.3.3 Stop 按钮与 textarea 文字不重叠

---

## 二、消息显示

### 2.1 流式打字机
- [x] 2.1.1 发送 prompt 后，文本逐字符/逐块出现
- [x] 2.1.2 打字机过程中无闪烁、无抖动
- [x] 2.1.3 打字机过程中自动滚动到底部

### 2.2 Markdown 渲染
- [ ] 2.2.1 发送 "用 markdown 写一个标题和代码块" 后正确渲染
  - 测试 prompt: `"用 markdown 写一个标题和代码块"`
- [ ] 2.2.2 代码块使用等宽字体，支持水平滚动
- [ ] 2.2.3 行内代码 `code` 有灰色背景圆角

### 2.3 用户消息
- [ ] 2.3.1 用户消息靠右对齐，灰色圆角气泡
- [ ] 2.3.2 连续发送多条用户消息，每条独立显示

---

## 三、Thinking 块

### 3.1 显示条件
- [ ] 3.1.1 发送需要推理的 prompt
  - 测试 prompt: `"分析这段代码的时间复杂度 O(n) = n^2 是否正确"`
- [ ] 3.1.2 发送简单 prompt "hello"，不出现 Thinking 块

### 3.2 折叠/展开
- [ ] 3.2.1 Thinking 块默认折叠，显示前 80 字符预览
- [ ] 3.2.2 点击 Thinking 块展开，显示完整内容
- [ ] 3.2.3 再次点击折叠

---

## 四、工具调用卡片

### 4.1 显示条件
- [ ] 4.1.1 发送 "读取 package.json 文件"
  - 测试 prompt: `"读取 package.json 文件"`
- [ ] 4.1.2 发送 "列出当前目录文件"
  - 测试 prompt: `"列出当前目录文件"`

### 4.2 卡片渲染
- [ ] 4.2.1 工具卡片显示工具名 + 状态标签
- [ ] 4.2.2 卡片显示 JSON 参数
- [ ] 4.2.3 工具执行成功，状态变为绿色 `success`
- [ ] 4.2.4 工具执行失败，状态变为红色 `error`

---

## 五、Stop 按钮功能

- [ ] 5.1 生成过程中点击 ⏹ Stop
- [ ] 5.2 Stop 后可以立即发送新消息
- [ ] 5.3 Stop 后消息列表保留已生成的部分

---

## 六、连接状态

### 6.1 初始连接
- [ ] 6.1.1 扩展激活后显示 "Connecting to DeepSeek TUI..."
- [ ] 6.1.2 连接成功后横幅消失，输入框可用
- [ ] 6.1.3 状态栏显示模型名、Turn 0、sessionId 前 8 位

### 6.2 TUI 崩溃恢复
- [ ] 6.2.1 手动 kill deepseek-tui 进程
  - 方法: `taskkill /f /im deepseek-tui.exe`
- [ ] 6.2.2 重连成功后恢复正常
- [ ] 6.2.3 3 次重试均失败后显示 "TUI crashed" 错误

---

## 七、消息持久化

- [ ] 7.1 发送 3 条用户消息 + 等待回复
- [ ] 7.2 关闭 VS Code，重新打开，之前的消息全部恢复显示
- [ ] 7.3 清空聊天后关闭再打开，空列表

---

## 八、调试日志检查（Developer Tools → Output → Celest）

### 8.1 session/update 结构
- [ ] 8.1.1 查找 `[DEBUG] session/update content keys:` 日志行
- [ ] 8.1.2 查找 `[DEBUG] tuiText:` `[DEBUG] tuiReasoning:` `[DEBUG] tuiToolCall:` 日志
- [ ] 8.1.3 查找 `[DEBUG] Unknown content type` 日志

### 8.2 时序验证
- [ ] 8.2.1 对比 `[DEBUG] sendPrompt start` 和 `session/update received` 时间

---

## 九、边界情况

- [ ] 9.1 空输入按 Enter — 不发送
- [ ] 9.2 纯空格输入 — 不发送
- [ ] 9.3 输入超长文本（500+ 字）— 不影响性能
- [ ] 9.4 快速连续发送 3 条消息 — 不丢消息
- [ ] 9.5 生成中关闭 VS Code — 不崩溃
- [ ] 9.6 包含特殊字符的输入 — 不执行脚本
  - 测试 prompt: `"<script>alert(1)</script> 解释这段代码"`
- [ ] 9.7 包含 emoji 的输入（😀🔥👍）— 正常发送和显示
- [ ] 9.8 包含中文 + 英文 + 代码混合输入 — 正常显示
- [ ] 9.9 暗色 / 亮色主题切换 — 颜色跟随 VS Code 主题
- [ ] 9.10 面板宽度拖动 — 布局自适应

---

## 十、Phase 1-2 问题汇总

| # | 严重度 | 问题描述 | 复现步骤 | 截图 |
|---|--------|---------|---------|------|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |

---

## Phase 3 追加测试用例

### 十一、@ 提及自动补全

#### 11.1 弹窗触发
- [ ] 11.1.1 在空输入框中输入 `@`，弹窗出现
- [ ] 11.1.2 输入 `@pac`，弹窗过滤显示匹配文件
- [ ] 11.1.3 输入 `@zzznotexist`，弹窗显示 "No matching files"
- [ ] 11.1.4 输入非 `@` 开头的文本，弹窗不出现

#### 11.2 键盘导航
- [ ] 11.2.1 弹窗可见时，按 `↓` 选中下一项
- [ ] 11.2.2 弹窗可见时，按 `↑` 选中上一项
- [ ] 11.2.3 按 `Enter` 选中当前高亮项
- [ ] 11.2.4 按 `Esc` 关闭弹窗

#### 11.3 鼠标交互
- [ ] 11.3.1 鼠标 hover 到某项，该项高亮
- [ ] 11.3.2 鼠标点击某项，选中并替换文本

### 十二、/ 命令面板

#### 12.1 弹窗触发
- [ ] 12.1.1 在输入框行首输入 `/`，弹窗出现
- [ ] 12.1.2 在空格后输入 `/`，弹窗出现
- [ ] 12.1.3 在单词中间输入 `/`（如 `path/to/file`），不触发

#### 12.2 命令内容
- [ ] 12.2.1 弹窗头显示 "Commands (N of 60)"
- [ ] 12.2.2 每条显示命令名 + 分类标签 + 描述
- [ ] 12.2.3 输入 `/com`，过滤显示匹配项
- [ ] 12.2.4 输入 `debug`，按分类过滤

#### 12.3 选择操作
- [ ] 12.3.1 Enter 选择命令
- [ ] 12.3.2 选择命令后弹窗关闭，光标在命令后

#### 12.4 互斥
- [ ] 12.4.1 @ 弹窗和 / 弹窗不同时显示

### 十三、Work 面板

- [ ] 13.1.1 发送 prompt 让 TUI 使用 `todo_write`
  - 测试 prompt: `"创建一个 todo list：1. 分析代码 2. 修复 bug 3. 测试"`
- [ ] 13.2.1 任务按状态排序：in_progress → pending → completed
- [ ] 13.2.2 任务状态标签正确着色
- [ ] 13.2.3 completed 任务有删除线
- [ ] 13.3.1 TUI 更新 todo 状态后，面板实时反映

### 十四、Plan 面板

- [ ] 14.1.1 explanation 顶部引用块显示
- [ ] 14.1.2 步骤列表显示状态图标 + 文本 + 标签
- [ ] 14.1.3 无计划数据时空状态提示

### 十五、Tab 布局

- [ ] 15.1 连接成功后 Tab 栏出现（Chat/Work/Plan）
- [ ] 15.2 默认在 Chat Tab
- [ ] 15.3 切换 Tab 不丢失数据
- [ ] 15.4 New Session 时 Work/Plan 数据清空
- [ ] 15.5 输入框在所有 Tab 下可见可用

### 十六、Sessions TreeView

- [ ] 16.1 首次打开无历史数据时显示提示
- [ ] 16.2 发送 prompt 后刷新 TreeView
- [ ] 16.3 条目显示 title 或 `Thread {id前8位}`
- [ ] 16.4 日期显示为本地化格式
- [ ] 16.5 tooltip 包含 Model + Mode 信息
- [ ] 16.6 点击条目聚焦到聊天输入框
- [ ] 16.7 TUI 不可用时显示错误提示

### 十七、chatViewProvider 文件列表

- [ ] 17.1 WebView 发送 `getFiles` 后返回文件列表
- [ ] 17.2 文件列表排除 node_modules/.git/target
- [ ] 17.3 文件列表限制 200 项

### 十八、Stop/Cancel — interrupt API 验证

- [ ] 18.1 生成中点 Stop，服务端收到 interrupt 请求
  - 验证: Output 日志 `[Thread] interrupt sent successfully`
- [ ] 18.2 interrupt 成功后生成立即停止
- [ ] 18.3 interrupt 不可用时 fallback 到 abort
- [ ] 18.4 连续点击 Stop 不崩溃

### 十九、@ / 弹窗键盘事件边界

- [ ] 19.1 弹窗可见时 Enter 不发送消息
- [ ] 19.2 弹窗可见时 Shift+Enter 正常换行
- [ ] 19.3 弹窗关闭后 Enter 恢复发送
- [ ] 19.4 弹窗中按 ↑ 到顶不溢出
- [ ] 19.5 弹窗中按 ↓ 到底不溢出

### 二十、toolName 映射验证

- [ ] 20.1 工具执行完毕后 toolResult 消息包含 toolName
- [ ] 20.2 todo_write 工具结果触发 Work 面板更新
- [ ] 20.3 update_plan 工具结果触发 Plan 面板更新
- [ ] 20.4 未知工具不会触发面板更新

---

### Phase 3 问题汇总

| # | 严重度 | 问题描述 | 复现步骤 | 截图 |
|---|--------|---------|---------|------|
| 1 | | | | |
| 2 | | | | |

**Phase 3 测试完成签名:** _____________  
**测试日期:** _____________  
**发现问题总数:** _____

---

## Phase 4 — 审批 + 执行 集成测试用例

> ⚠️ **审批触发条件：** 只读工具 TUI 不要求审批。用写入/执行类工具测试。
> 推荐测试 prompt：
> - **exec_shell:** `"运行命令 echo hello"`
> - **write_file:** `"创建 test.txt 文件，内容为 hello world"`

### 🔐 二十一、审批弹窗显示

- [x] 21.1 发送 prompt 后审批弹窗弹出
  - 测试 prompt: `"运行命令 echo hello"`
  - 实际：ok
- [x] 21.2 弹窗显示正确的工具名 `exec_shell`
- [x] 21.3 弹窗显示审批描述（命令内容）
- [x] 21.4 弹窗显示倒计时 `Auto-deny in 5:00`
- [x] 21.5 三个决策按钮均可用: Allow / Allow Session / Deny

### ✅ 二十二、Allow 决策

- [x] 22.1 点击 "Allow" 按钮，弹窗关闭，TUI 继续执行
- [x] 22.2 Allow 后工具执行结果正常显示
- [x] 22.3 Allow 后下一次工具调用重新弹出审批窗

### ✨ 二十三、Allow (Session) 决策

- [x] 23.1 点击 "Allow (Session)" 按钮
- [x] 23.2 Allow Session 后同一线程不再弹出审批窗

### ❌ 二十四、Deny 决策

- [x] 24.1 点击 "Deny" 按钮，工具卡片显示 error
- [x] 24.2 Deny 后 LLM 收到拒绝反馈并尝试替代方案

### ⏱ 二十五、审批超时

- [ ] 25.1 等待倒计时归零后自动 Deny
- [ ] 25.2 超时后控制台显示 `approval.timeout` 事件

### 📺 二十六、Shell 输出实时流

- [x] 26.1 exec_shell 工具卡片实时展示输出
  - 测试 prompt: `"运行命令 echo hello 并逐行输出 1 到 5"`
- [x] 26.2 Shell 输出流式追加无覆盖
- [x] 26.3 Shell 输出完成后状态更新

### 📄 二十七、Diff 预览

- [x] 27.1 write_file 完成后出现 "View Diff" 按钮
  - 测试 prompt: `"在项目根目录创建一个 test_celest.txt 文件，内容为 Hello Celest"`
- [x] 27.2 点击 "View Diff" 打开 VS Code diff editor
- [x] 27.3 edit_file 完成后也显示 View Diff
- [x] 27.4 非文件修改工具不显示 View Diff

### 🎯 二十八、完整审批流程 E2E

- [ ] 28.1 完整流程：发 prompt → 审批弹出 → Allow → 执行 → 结果
- [ ] 28.2 拒绝后重试：Deny → LLM 重试 → 新审批 → Allow

### 📌 二十九、Tasks 面板

- [x] 29.1 后台任务自动出现在 Tasks 面板
- [x] 29.2 任务状态实时更新
- [x] 29.3 /clear 和 newSession 清空面板

---

### Phase 4 问题汇总

| # | 严重度 | 问题描述 | 复现步骤 | 截图 |
|---|--------|---------|---------|------|
| 1 | | | | |
| 2 | | | | |

**Phase 4 测试完成签名:** _____________  
**测试日期:** _____________  
**发现问题总数:** _____

---

## Phase 5 — 配置 + 模型 + 国际化 集成测试用例

> ⚠️ **Phase 5 新增功能:** SettingsPanel 设置面板、模型切换下拉框、API Key 安全存储、二进制自动下载、i18n 国际化。
> 全部测试 prompt 和验证方法如下。

---

### 三十、设置面板 UI

#### 30.1 打开/关闭

- [ ] 30.1.1 点击标题栏 ⚙ 按钮打开设置面板
  - 预期：弹窗出现，半透明遮罩覆盖背景，面板居中
  - 实际：_______________
- [ ] 30.1.2 点击遮罩区域关闭面板（不保存）
  - 实际：_______________
- [ ] 30.1.3 点击 ✕ 按钮关闭面板
  - 实际：_______________
- [ ] 30.1.4 通过命令面板 `Celest: Open Settings` 打开
  - 方法：`Ctrl+Shift+P` → 输入 `Celest Open Settings` → 回车
  - 实际：_______________

#### 30.2 Tab 切换

- [ ] 30.2.1 默认展示"通用"Tab（API Base URL、API Key、语言、二进制路径）
  - 实际：_______________
- [ ] 30.2.2 点击"模型"Tab（默认模型、推理深度、提供商）
  - 实际：_______________
- [ ] 30.2.3 点击"关于"Tab（版本号、TUI 版本、Node 版本、VS Code 版本）
  - 实际：_______________

#### 30.3 配置项交互

- [ ] 30.3.1 API Base URL 输入框可编辑 → 保存后 settings.json 中 `celest.apiBase` 更新
  - 验证：打开 `%APPDATA%\Code\User\settings.json` 检查
  - 实际：_______________
- [ ] 30.3.2 二进制路径 `...` 浏览按钮弹出文件选择器
  - 实际：_______________

---

### 三十一、模型选择器

**测试入口:** 标题栏模型下拉框（V4 Pro / V4 Flash）

- [x] 31.1 TUI 连接后下拉框可见，默认 `V4 Flash`
  - 实际：_______________
- [x] 31.2 下拉框包含 `V4 Pro` 和 `V4 Flash` 两个选项
  - 实际：_______________
- [ ] 31.3 切换到 V4 Pro 后发送 prompt 验证
  - **测试 prompt:** `"你是什么模型？"`
  - **验证方法:** Output 日志 → 搜索 `model=deepseek-v4-pro`
  - 预期：日志显示 model=deepseek-v4-pro，回复中包含模型名
  - 实际：_______________
- [ ] 31.4 切换到 V4 Flash 后发送 prompt 验证
  - **测试 prompt:** `"你是什么模型？"`
  - 预期：日志显示 model=deepseek-v4-flash
  - 实际：_______________
- [x] 31.5 切换模型后 ContextBar 同步更新
  - 预期：底部状态栏模型名随下拉框变化
  - 实际：_______________

---

### 三十二、API Key 安全存储

> 测试前先清除已有 Key：打开设置面板 → 清空 Key → 保存

- [ ] 32.1 未设置 Key 时显示红色"未设置"状态
  - 实际：_______________
- [ ] 32.2 输入 API Key 并保存 → 状态变为绿色"已设置 ✓"
  - 实际：_______________
- [ ] 32.3 关闭 VS Code 重新打开 → Key 仍存在（SecretStorage 持久化）
  - 实际：_______________
- [ ] 32.4 Key 输入框默认以 password 类型显示（圆点）
  - 实际：_______________
- [ ] 32.5 点击 👁 按钮切换显示/隐藏
  - 实际：_______________
- [ ] 32.6 清空 Key 并保存 → 状态恢复"未设置"
  - 实际：_______________
- [ ] 32.7 设置 Key 后重启 TUI → 日志出现 `[Config] passing DEEPSEEK_API_KEY via env`
  - **验证方法:** Output 面板 → Celest channel
  - 实际：_______________
- [ ] 32.8 TUI 使用 Key 成功调用 API
  - **测试 prompt:** `"hello"`
  - 预期：正常收到回复（无 401 错误）
  - 实际：_______________

---

### 三十三、二进制自动下载

> 测试前确认 `celest.binaryPath` 为空且 `celest.autoDownloadBinary` 为 true

- [ ] 33.1 二进制不存在时弹出下载提示
  - 预期：通知含 Download Now / Locate Manually / Cancel 三按钮
  - 实际：_______________
- [x] 33.2 点击 Download Now → 日志显示 `[BinaryDownloader] downloading from:`
  - **验证方法:** Output → Celest
  - 实际：_______________
- [ ] 33.3 下载完成后 `celest.binaryPath` 自动更新
  - **验证:** `Ctrl+,` → 搜索 `celest.binaryPath`
  - 实际：_______________
- [ ] 33.4 下载完成后 TUI 自动启动 → Connecting → Connected
  - 实际：_______________
- [ ] 33.5 点击 Locate Manually → 弹出文件选择器 → 路径保存
  - 实际：_______________
- [ ] 33.6 点击 Cancel → 不下载，TUI 不启动
  - 实际：_______________
- [ ] 33.7 "关于"Tab 点击"下载二进制"按钮
  - 实际：_______________
- [ ] 33.8 "关于"Tab 显示当前 TUI 版本号（非"未知"）
  - 实际：_______________

---

### 三十四、i18n 国际化

#### 34.1 语言切换

- [ ] 34.1.1 默认语言为简体中文
  - 预期：设置面板标题"Celest 设置"，连接横幅"正在连接 DeepSeek TUI..."
  - 实际：_______________
- [ ] 34.1.2 设置面板中切换语言为 English 并保存
  - 预期：面板标题变为 "Celest Settings"
  - 实际：_______________
- [ ] 34.1.3 切换回简体中文
  - 实际：_______________

#### 34.2 翻译覆盖

- [ ] 34.2.1 中文环境 Tab 标签："通用"、"模型"、"关于"
  - 实际：_______________
- [ ] 34.2.2 英文环境 Tab 标签："General"、"Model"、"About"
  - 实际：_______________
- [ ] 34.2.3 版本号等数值不随语言变化
  - 实际：_______________

#### 34.3 不可翻译项验证

- [ ] 34.3.1 模型下拉框始终显示 `V4 Pro` / `V4 Flash`
  - 实际：_______________
- [ ] 34.3.2 工具描述保持 TUI 原样透传
  - **测试 prompt:** `"列出当前目录的文件"`
  - 预期：工具卡片显示 `list_dir`（不翻译工具名）
  - 实际：_______________

---

### 三十五、配置持久化

- [ ] 35.1 修改默认模型为 V4 Pro → 保存 → 重启 VS Code → 模型保持 V4 Pro
  - **验证:** 重启后查看标题栏下拉框
  - 实际：_______________
- [ ] 35.2 修改语言为 English → 保存 → 重启 → UI 英文
  - 实际：_______________
- [ ] 35.3 修改 API Base URL → 保存 → 重启 → 值保持
  - 实际：_______________
- [ ] 35.4 修改推理深度 → 保存 → 重启 → 值保持
  - 实际：_______________

---

### 三十六、Runtime Info 集成

- [ ] 36.1 TUI 连接后"关于"Tab 显示实际 TUI 版本号
  - 预期：非"未知"，如 `0.8.40`
  - 实际：_______________
- [ ] 36.2 TUI 未连接时显示"未知"
  - 实际：_______________

---

### 三十七、模型切换与 Thread 兼容性

**测试流程:** V4 Flash 发消息 → 切换 V4 Pro → 继续对话

- [ ] 37.1 同一 thread 中切换模型
  - 步骤：发 `"hello"` (Flash) → 切 V4 Pro → 发 `"你是什么模型？"`
  - 预期：第二条回复显示 V4 Pro
  - 实际：_______________
- [ ] 37.2 New Session 后模型保持上次选择（不回到默认）
  - 实际：_______________

---

### 三十八、设置面板 ESC 关闭

- [ ] 38.1 设置面板打开时按 Esc → 面板关闭，不保存修改
  - 实际：_______________
- [ ] 38.2 面板关闭后聚焦回到输入框
  - 实际：_______________

---

### 三十八、Mode 模式切换

**测试入口:** ContextBar 底部状态栏 mode 区域

- [ ] 38.1 默认显示 `Agent` 模式
  - 实际：_______________
- [ ] 38.2 点击 mode 切换到 `Plan`
  - 预期：后端日志 `[Mode] switching to plan`
  - 实际：_______________
- [ ] 38.3 点击 mode 切换到 `YOLO`
  - 预期：`autoApprove = true`，后续工具不弹审批
  - 实际：_______________
- [ ] 38.4 YOLO 模式下发 prompt 验证
  - **测试 prompt:** `"创建 test_yolo.txt，内容为 yolo mode test"`
  - 预期：直接执行，无审批弹窗
  - 实际：_______________
- [ ] 38.5 切回 Agent 模式后审批恢复
  - **测试 prompt:** `"运行命令 echo test"`
  - 预期：审批弹窗正常弹出
  - 实际：_______________

---

### 三十九、完整配置流程 E2E

- [ ] 39.1 端到端配置流程
  - 步骤：
    1. 打开设置面板（⚙）
    2. 输入 API Key → 保存
    3. 切换默认模型为 V4 Pro → 保存
    4. 切换语言为 English → 保存
    5. 关闭面板
    6. 发送 prompt: `"hello"`
  - 预期：全程无报错，回复正常，UI 为英文
  - 实际：_______________

---

### 四十、Phase 5 问题汇总

| # | 严重度 | 问题描述 | 复现步骤 | 截图 |
|---|--------|---------|---------|------|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |

---

**Phase 5 测试完成签名:** _____________  
**测试日期:** _____________  
**发现问题总数:** _____
