# Celest Phase 2 / Phase 3 — 集成测试用例清单

> 手动测试用。每项测试后记录 实际结果 + 截图路径。
> 发现问题后在末尾「问题汇总」区追加。

---

## 测试环境

| 项目 | 值 |
|------|-----|
| 日期 | ____________ |
| 测试人 | ____________ |
| VS Code 版本 | ____________ |
| Celest 构建产物 | ext: ____ KB / gui: ____ KB |
| deepseek-tui 版本 | (Output → Celest → `ACP initialized:`) |
| OS | Windows 11 / 其他: ________ |

---

## 一、UI 布局

### 1.1 整体布局
- [x] 1.1.1 打开 Celest 面板后，从上到下依次看到：标题栏 → 空状态 → 输入区 → 状态栏
  - 预期：四层垂直排列，无重叠、无溢出
  - 实际：ok
- [x] 1.1.2 面板宽度缩窄至 300px 时，布局不崩坏
  - 预期：输入框自适应缩小，消息文字正常换行
  - 实际：ok

### 1.2 输入区域
- [x] 1.2.1 输入框最小高度约 60px（3 行），placeholder 文字可见
  - 预期：`Ask anything... (@ to reference files, / for commands)` 完整显示
  - 实际：ok
- [x] 1.2.2 输入多行文本（5 行以上），输入框自动增高但不超过 200px
  - 预期：高度随内容增长，到 200px 后出现内部滚动
  - 实际：ok
- [x] 1.2.3 输入区底部显示 "Enter to send, Shift+Enter for newline" 提示
  - 预期：灰色小字，左对齐
  - 实际：ok
- [x] 1.2.4 Send 按钮位于输入区右下角
  - 预期：蓝色主题按钮，输入为空时半透明禁用
  - 实际：ok

### 1.3 Stop 按钮
- [x] 1.3.1 空闲时 Stop 按钮不可见
  - 预期：输入框右上角无红色按钮
  - 实际：ok
- [x] 1.3.2 生成中 Stop 按钮出现在输入框右上角
  - 预期：红色按钮 `⏹ Stop`，浮动在 textarea 内部右上角
  - 实际：ok
- [x] 1.3.3 Stop 按钮与 textarea 文字不重叠
  - 预期：textarea 右侧 padding 约 60px，文字在按钮左侧
  - 实际：ok

---

## 二、消息显示

### 2.1 流式打字机
- [x] 2.1.1 发送 prompt 后，文本逐字符/逐块出现
  
  - 预期：文字一个一个出现，不是一次性全部显示
  - 实际：ok
  
- [x] 2.1.2 打字机过程中无闪烁、无抖动
  - 预期：追加文本时已渲染部分保持稳定
  - 实际：ok
- [x] 2.1.3 打字机过程中自动滚动到底部
  - 预期：新文字出现时视口始终在底部
  - 实际：ok

### 2.2 Markdown 渲染
- [ ] 2.2.1 发送 "用 markdown 写一个标题和代码块" 后正确渲染
  - 预期：`# 标题` 显示为大号粗体，代码块有语法高亮背景
  - 实际：_______________
- [ ] 2.2.2 代码块使用等宽字体，支持水平滚动
  - 预期：长代码行不折行，出现水平滚动条
  - 实际：_______________
- [ ] 2.2.3 行内代码 `code` 有灰色背景圆角
  - 预期：`` `text` `` 渲染为灰色底圆角标签
  - 实际：_______________

### 2.3 用户消息
- [ ] 2.3.1 用户消息靠右对齐，灰色圆角气泡
  - 预期：右对齐，`max-width: 90%`，引用块背景色
  - 实际：_______________
- [ ] 2.3.2 连续发送多条用户消息，每条独立显示
  - 预期：每条新消息追加到底部，不覆盖之前的
  - 实际：_______________

---

## 三、Thinking 块

### 3.1 显示条件
- [ ] 3.1.1 发送需要推理的 prompt："分析这段代码的时间复杂度 O(n) = n^2 是否正确"
  - 预期：回复中出现 "🧠 Thinking" 折叠块（如果 TUI 发送 reasoning）
  - 实际：_______________
- [ ] 3.1.2 发送简单 prompt "hello"，不出现 Thinking 块
  - 预期：简单对话通常不触发 thinking
  - 实际：_______________

### 3.2 折叠/展开
- [ ] 3.2.1 Thinking 块默认折叠，显示前 80 字符预览
  - 预期：`🧠 Thinking  Let me analyze...` 预览文字后面有 `...`
  - 实际：_______________
- [ ] 3.2.2 点击 Thinking 块展开，显示完整内容
  - 预期：展开后内容半透明（`opacity: 0.85`），可与正文区分
  - 实际：_______________
- [ ] 3.2.3 再次点击折叠
  - 预期：收起回预览状态
  - 实际：_______________

---

## 四、工具调用卡片

### 4.1 显示条件
- [ ] 4.1.1 发送 "读取 package.json 文件"
  - 预期：出现 `🔧 read_file` 工具卡片（如果 TUI 发送 toolCall）
  - 实际：_______________
- [ ] 4.1.2 发送 "列出当前目录文件"
  - 预期：出现 `🔧 list_dir` 工具卡片（如果 TUI 执行了工具）
  - 实际：_______________

### 4.2 卡片渲染
- [ ] 4.2.1 工具卡片显示工具名 + 状态标签
  - 预期：`🔧 read_file  ───── pending`
  - 实际：_______________
- [ ] 4.2.2 卡片显示 JSON 参数
  - 预期：参数区域有灰色代码块背景，格式化 JSON
  - 实际：_______________
- [ ] 4.2.3 工具执行成功，状态变为绿色 `success`
  - 预期：状态标签变绿，Result 区域显示输出
  - 实际：_______________
- [ ] 4.2.4 工具执行失败，状态变为红色 `error`
  - 预期：状态标签变红，显示错误信息
  - 实际：_______________

---

## 五、Stop 按钮功能

- [ ] 5.1 生成过程中点击 ⏹ Stop
  - 预期：生成停止，按钮消失，输入框恢复可用
  - 实际：_______________
- [ ] 5.2 Stop 后可以立即发送新消息
  - 预期：输入框可用，Enter 正常发送
  - 实际：_______________
- [ ] 5.3 Stop 后消息列表保留已生成的部分
  - 预期：不会清空已显示的文本
  - 实际：_______________

---

## 六、连接状态

### 6.1 初始连接
- [ ] 6.1.1 扩展激活后显示 "Connecting to DeepSeek TUI..."
  - 预期：连接横幅出现在输入区上方
  - 实际：_______________
- [ ] 6.1.2 连接成功后横幅消失，输入框可用
  - 预期：横幅消失，`tuiReady` 状态切换
  - 实际：_______________
- [ ] 6.1.3 状态栏显示模型名、Turn 0、sessionId 前 8 位
  - 预期：`deepseek-v4-flash | Turn 0 | a1b2c3d4`
  - 实际：_______________

### 6.2 TUI 崩溃恢复
- [ ] 6.2.1 手动 kill deepseek-tui 进程：`taskkill /f /im deepseek-tui.exe`
  - 预期：UI 显示提示，约 2 秒后自动尝试重连
  - 实际：_______________
- [ ] 6.2.2 重连成功后恢复正常
  - 预期：输入框恢复可用，可以继续对话
  - 实际：_______________
- [ ] 6.2.3 3 次重试均失败后显示 "TUI crashed" 错误
  - 预期：显示错误消息，不再重试
  - 实际：_______________

---

## 七、消息持久化

- [ ] 7.1 发送 3 条用户消息 + 等待回复
  - 实际：_______________
- [ ] 7.2 关闭 VS Code，重新打开
  - 预期：之前的消息全部恢复显示
  - 实际：_______________
- [ ] 7.3 清空聊天后关闭再打开
  - 预期：空列表，不恢复已清除的消息
  - 实际：_______________

---

## 八、调试日志检查（Developer Tools → Output → Celest）

### 8.1 session/update 结构
- [ ] 8.1.1 查找 `[DEBUG] session/update content keys:` 日志行
  - 记录实际键名：_______________
  - 如有 `reasoning`、`toolCall`、`toolResult`，说明结构化内容可用
  - 如只有 `text`，说明 TUI 端需要改造
- [ ] 8.1.2 查找 `[DEBUG] tuiText:` `[DEBUG] tuiReasoning:` `[DEBUG] tuiToolCall:` 日志
  - 记录命中了哪个分支：_______________
- [ ] 8.1.3 查找 `[DEBUG] Unknown content type` 日志
  - 如有，记录未知键名：_______________

### 8.2 时序验证
- [ ] 8.2.1 对比 `[DEBUG] sendPrompt start` 和 `session/update received` 时间
  - 记录：_______________
  - 如 session/update 在 sendPrompt end 之后才到达 → 不是真流式
  - 如在之前到达 → 是真流式

---

## 九、边界情况

- [ ] 9.1 空输入按 Enter — 不发送
- [ ] 9.2 纯空格输入 — 不发送
- [ ] 9.3 输入超长文本（500+ 字）— 不影响性能
- [ ] 9.4 快速连续发送 3 条消息 — 不丢消息，不卡死
- [ ] 9.5 生成中关闭 VS Code — 不崩溃（进程被 kill）
- [ ] 9.6 包含特殊字符的输入（`<script>alert(1)</script>`）— 不执行脚本
- [ ] 9.7 包含 emoji 的输入（😀🔥👍）— 正常发送和显示
- [ ] 9.8 包含中文 + 英文 + 代码混合输入 — 正常显示
- [ ] 9.9 暗色 / 亮色主题切换 — 颜色跟随 VS Code 主题
- [ ] 9.10 面板宽度在 250px ~ 800px 之间拖动 — 布局自适应

---

## 十、问题汇总

> 逐条记录，每项标注严重度：🔴Critical / 🟡Major / 🟢Minor

| # | 严重度 | 问题描述 | 复现步骤 | 截图 |
|---|--------|---------|---------|------|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |
| 4 | | | | |
| 5 | | | | |

---

**测试完成签名:** _____________  
**测试日期:** _____________  
**发现问题总数:** _____

---

## Phase 3 追加测试用例

### 十一、@ 提及自动补全

#### 11.1 弹窗触发
- [ ] 11.1.1 在空输入框中输入 `@`，弹窗出现
  - 预期：`Mention files` 弹窗出现在输入框上方
  - 实际：_______________
- [ ] 11.1.2 输入 `@pac`，弹窗过滤显示匹配文件
  - 预期：只显示文件名包含 "pac" 的文件（如 package.json）
  - 实际：_______________
- [ ] 11.1.3 输入 `@` 后继续输入不存在的文件名 `@zzznotexist`
  - 预期：弹窗显示 "No matching files"
  - 实际：_______________
- [ ] 11.1.4 输入非 `@` 开头的文本，弹窗不出现
  - 预期：正常输入，无弹窗
  - 实际：_______________

#### 11.2 键盘导航
- [ ] 11.2.1 弹窗可见时，按 `↓` 选中下一项
  - 预期：高亮项下移，颜色变为 `activeSelectionBackground`
  - 实际：_______________
- [ ] 11.2.2 弹窗可见时，按 `↑` 选中上一项
  - 预期：高亮项上移
  - 实际：_______________
- [ ] 11.2.3 按 `Enter` 选中当前高亮项
  - 预期：`@filterText` 被替换为 `@relativePath `，弹窗关闭，光标在路径后
  - 实际：_______________
- [ ] 11.2.4 按 `Esc` 关闭弹窗
  - 预期：弹窗消失，保留当前输入的 `@xxx` 文本
  - 实际：_______________

#### 11.3 鼠标交互
- [ ] 11.3.1 鼠标 hover 到某项，该项高亮
  - 预期：hover 样式生效
  - 实际：_______________
- [ ] 11.3.2 鼠标点击某项
  - 预期：选中该项，替换文本
  - 实际：_______________

#### 11.4 文件信息显示
- [ ] 11.4.1 弹窗每项显示文件图标 + 文件名 + 相对路径
  - 预期：`📄 file.ts  src/components/file.ts`
  - 实际：_______________
- [ ] 11.4.2 列表最多显示 20 项
  - 预期：输入 `@` 时不溢出
  - 实际：_______________

### 十二、/ 命令面板

#### 12.1 弹窗触发
- [ ] 12.1.1 在输入框行首输入 `/`，弹窗出现
  - 预期：`Commands` 弹窗显示 6 个命令
  - 实际：_______________
- [ ] 12.1.2 在空格后输入 `/`，弹窗出现
  - 预期：`hello /help` 中检测到 `/help`
  - 实际：_______________
- [ ] 12.1.3 在单词中间输入 `/`（如 `path/to/file`），不触发
  - 预期：路径中的 `/` 不触发命令弹窗
  - 实际：_______________

#### 12.2 命令内容（60 个命令，7 分类，对齐 TUI 0.8.40）
- [ ] 12.2.1 弹窗头显示 "Commands (N of 60)"
  - 预期：N 随过滤动态变化
  - 实际：_______________
- [ ] 12.2.2 每条显示命令名 + 分类标签 + 描述
  - 预期：`/compact  SESSION  Compact conversation context`
  - 实际：_______________
- [ ] 12.2.3 输入 `/com`，过滤显示匹配项（含 `/compact`）
  - 预期：同时匹配名称和分类的条目
  - 实际：_______________
- [ ] 12.2.4 输入 `debug`，按分类过滤
  - 预期：显示所有 debug 分类命令
  - 实际：_______________

#### 12.3 选择操作
- [ ] 12.3.1 Enter 选择命令
  - 预期：`/filterText` 替换为 `/command `
  - 实际：_______________
- [ ] 12.3.2 选择命令后弹窗关闭，光标在命令后
  - 预期：可以继续输入参数
  - 实际：_______________

#### 12.4 互斥
- [ ] 12.4.1 @ 弹窗和 / 弹窗不同时显示
  - 预期：输入 `@` 时 / 弹窗关闭，反之亦然
  - 实际：_______________

### 十三、Work 面板

#### 13.1 触发条件
- [ ] 13.1.1 发送 prompt 让 TUI 使用 `todo_write`
  - 预期：Tab 栏出现，Work badge 显示数量
  - 实际：_______________
- [ ] 13.1.2 无 todo_write 数据时 Work Tab 仍可见但为空
  - 预期：显示 "No active tasks" 空状态
  - 实际：_______________

#### 13.2 数据显示
- [ ] 13.2.1 任务按状态排序：in_progress → pending → completed
  - 预期：🔄 任务在前，⬜ 中间，✅ 在后
  - 实际：_______________
- [ ] 13.2.2 任务状态标签正确着色
  - 预期：in_progress=蓝色, pending=灰色, completed=绿色
  - 实际：_______________
- [ ] 13.2.3 completed 任务有删除线
  - 预期：文字 `text-decoration: line-through`
  - 实际：_______________

#### 13.3 动态更新
- [ ] 13.3.1 TUI 更新 todo 状态后，面板实时反映
  - 预期：同一回合中 todo 从不 pending 变成 in_progress
  - 实际：_______________

### 十四、Plan 面板

#### 14.1 数据显示
- [ ] 14.1.1 explanation 顶部引用块显示
  - 预期：灰色背景 + 左边框，文字左对齐
  - 实际：_______________
- [ ] 14.1.2 步骤列表显示状态图标 + 文本 + 标签
  - 预期：与 Work 面板一致的状态样式
  - 实际：_______________
- [ ] 14.1.3 无计划数据时空状态提示
  - 预期："No active plan"
  - 实际：_______________

### 十五、Tab 布局

- [ ] 15.1 连接成功后 Tab 栏出现（Chat/Work/Plan）
  - 预期：三个按钮水平排列
  - 实际：_______________
- [ ] 15.2 默认在 Chat Tab
  - 预期：消息列表可见
  - 实际：_______________
- [ ] 15.3 切换 Tab 不丢失数据
  - 预期：Work 数据在切换回 Work Tab 后仍存在
  - 实际：_______________
- [ ] 15.4 New Session 时 Work/Plan 数据清空
  - 预期：tab badge 消失，面板恢复空状态
  - 实际：_______________
- [ ] 15.5 输入框在所有 Tab 下可见可用
  - 预期：不随 Tab 切换隐藏
  - 实际：_______________

### 十六、Sessions TreeView（适配 TUI 0.8.40 ThreadRecord）

- [ ] 16.1 首次打开无历史数据时显示提示
  - 预期："No sessions yet. Send a prompt to create one."
  - 实际：_______________
- [ ] 16.2 发送 prompt 后刷新 TreeView
  - 预期：出现新 session 条目（需手动点击刷新或切换标签触发）
  - 实际：_______________
- [ ] 16.3 条目显示 title（如有）或 `Thread {id前8位}`
  - 预期：TUI 设置过 title 时显示 title，否则显示 Thread 前缀
  - 实际：_______________
- [ ] 16.4 日期显示为 ISO 8601 转本地化格式（如 `May 21, 14:30`）
  - 预期：不再是 unix timestamp * 1000 的完整日期，而是短格式
  - 实际：_______________
- [ ] 16.5 tooltip 包含 Model + Mode 信息
  - 预期：hover 条目时显示 `Model: deepseek-v4-flash \nMode: agent \nUpdated: ...`
  - 实际：_______________
- [ ] 16.6 点击条目聚焦到聊天输入框
  - 预期：光标定位到 InputBox
  - 实际：_______________
- [ ] 16.7 TUI 不可用时显示错误提示
  - 预期：显示 "Error loading sessions" 而非崩溃
  - 实际：_______________

### 十八、Stop/Cancel — interrupt API 验证

- [ ] 18.1 生成中点 Stop，服务端收到 interrupt 请求
  - 预期：Output 日志出现 `[Thread] interrupt sent successfully` 或 `HTTP 200`
  - 实际：_______________
- [ ] 18.2 interrupt 成功后生成立即停止
  - 预期：`promptEnded` 消息触发，输入框恢复可用
  - 实际：_______________
- [ ] 18.3 interrupt 不可用时 fallback 到 abort
  - 预期：日志出现 `interrupt request failed`，随后 `aborted by user`
  - 实际：_______________
- [ ] 18.4 连续点击 Stop 不崩溃
  - 预期：多次 cancel() 调用不抛异常
  - 实际：_______________

### 十九、@ / 弹窗键盘事件边界

- [ ] 19.1 弹窗可见时 Enter 不发送消息
  - 预期：Enter 选中弹窗项，不触发 sendPrompt
  - 实际：_______________
- [ ] 19.2 弹窗可见时 Shift+Enter 正常换行
  - 预期：输入框中换行，弹窗不响应
  - 实际：_______________
- [ ] 19.3 弹窗关闭后 Enter 恢复发送
  - 预期：Esc 关闭弹窗 → 再按 Enter 正常发送消息
  - 实际：_______________
- [ ] 19.4 弹窗中按 ↑ 到顶不溢出
  - 预期：selectedIdx 停留在 0
  - 实际：_______________
- [ ] 19.5 弹窗中按 ↓ 到底不溢出
  - 预期：selectedIdx 停留在最后一项
  - 实际：_______________

### 二十、toolName 映射验证

- [ ] 20.1 工具执行完毕后 toolResult 消息包含 toolName
  - 预期：App.vue 收到 `msg.toolResult.toolName === 'read_file'`
  - 实际：_______________
- [ ] 20.2 todo_write 工具结果触发 Work 面板更新
  - 预期：toolName === 'todo_write' → parseTodoWrite 被调用
  - 实际：_______________
- [ ] 20.3 update_plan 工具结果触发 Plan 面板更新
  - 预期：toolName === 'update_plan' → parseUpdatePlan 被调用
  - 实际：_______________
- [ ] 20.4 未知工具不会触发面板更新
  - 预期：其他工具（如 read_file）不修改 Work/Plan 数据
  - 实际：_______________

### 十七、chatViewProvider 文件列表

- [ ] 17.1 WebView 发送 `getFiles` 后返回文件列表
  - 预期：console 输出 `fileList` 消息
  - 实际：_______________
- [ ] 17.2 文件列表排除 node_modules/.git/target
  - 预期：列表中无这些目录下的文件
  - 实际：_______________
- [ ] 17.3 文件列表限制 2000 项
  - 预期：超过时不崩溃
  - 实际：_______________

---

### Phase 3 问题汇总

| # | 严重度 | 问题描述 | 复现步骤 | 截图 |
|---|--------|---------|---------|------|
| 1 | | | | |
| 2 | | | | |

---

**Phase 3 测试完成签名:** _____________  
**测试日期:** _____________  
**发现问题总数:** _____
